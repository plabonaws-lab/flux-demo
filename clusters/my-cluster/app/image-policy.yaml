# clusters/my-cluster/app/image-policy.yaml
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: admin-panel
  namespace: default
spec:
  imageRepositoryRef:
    name: admin-panel
  policy:
    semver:
      range: "*"

